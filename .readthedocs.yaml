version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install uv
      - uv sync --only-group docs --no-install-project
    build:
      html:
        - uv run --no-sync sphinx-build -b html docs $READTHEDOCS_OUTPUT/html
