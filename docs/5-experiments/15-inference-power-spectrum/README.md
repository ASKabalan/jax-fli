# Experiment 15 — Power-spectrum-level inference vs theory ⚠️ BLOCKED

**Goal.** Power-spectrum-level inference — decoupled angular `C_ℓ` vs theory — on the same geometry
and mask as [Experiment 14](../14-inference-cosmogrid-shear/README.md) (observer `(0.1, 0.5, 0.9)`,
DES Y3 mask), using the power-spectrum forward model.

**Status.** ⚠️ **Blocked on the inference pipeline** (needs the power-spectrum model + decoupled
`C_ℓ`). Differentiated *inside* `fli-infer` (`--grad checkpoint`).

## Run

Blocked — see [`run.sh`](run.sh).
