#!/bin/bash
# Experiment 15 — power-spectrum-level inference vs theory. BLOCKED on the inference pipeline.
# Decoupled angular C_ℓ vs theory; same geometry/mask as Exp 14. Differentiated inside fli-infer.
set -euo pipefail
echo "Exp 15 is BLOCKED on the inference pipeline (fli-infer + power-spectrum model). No runs yet."
exit 1
